# Required evidence

Place the real screenshots generated from your environment in this folder:

- `terraform-plan.png` — successful `terraform plan`
- `terraform-apply.png` — successful `terraform apply` if you use an AWS account

Do not use a fabricated screenshot. The evidence should show the actual Terraform command and successful result from the environment where the project was tested.
